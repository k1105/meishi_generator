services:
  - type: web
    name: meishi-backend
    env: node
    plan: free
    buildCommand: |
      npm install
      npm run build
      pip install -r python-generator/requirements.txt
    startCommand: npm run start
    envVars:
      - fromGroup: meishi-env
